# Adversarial re-audit — 30 August 2026

Evidence triage after [REVIEW_2026-08-28.md](REVIEW_2026-08-28.md) and
slices [REVIEW_FIXES_2026-08-28.md](REVIEW_FIXES_2026-08-28.md) through
[REVIEW_FIXES_2026-08-28-6.md](REVIEW_FIXES_2026-08-28-6.md).

**This is not a seventh software-fix slice.** Do not create
`plan/refactor-adversarial-fixes-7.md`. Host-safe leftover class remains
empty (slice 6). `#71` / `#72` landed claim-safe README stills and
`teg-pwm-memory` `applyTo` for those paths.

Canvas (open beside the chat):
`C:\Users\lee\.cursor\projects\d-git-teg\canvases\adversarial-reaudit-2026-08-30.canvas.tsx`

---

## Verdict

| Gate | Status |
|---|---|
| Host-safe leftover class | Empty |
| Fresh host-testable regressions found this pass | None |
| Software fix PRs from this re-audit | **NO-GO** (default) |
| Docs-only claim drift | None beyond this note + memories |
| Remaining ship gate | `docs/BENCH_CHECKS.md` disconnected checklist (~96 open boxes) |
| Product posture | **NO-SHIP** until that checklist passes |

Host Unity (six-suite + `test_spectrum_wire_quantize_saturates` +
`test_mqtt_discovery` reported separately) is **not** ISR/OUTEN proof.
PLL remains a bench reference lock, not grid-tie. No HA energy-dashboard
product claims.

---

## How reviewed

1. Re-read slice-6 deferred table and stay-offs; treat settled host fixes as
   closed, not new P0s.
2. Spot-check current `main` contracts:
   - `releaseOutputInhibit()` / `vFaultGeneration` (`src/pwm_utils.cpp`)
   - `thermalConfigure()` keep-sample / mask-before-scan (`src/thermal.cpp`)
   - `mtpAllowsPwmRelease()` / begin-while-inhibited (`src/mtp_service.cpp`)
   - `restoreSecrets` identity gate (`src/config_serde.h`)
   - lite `meterActive` last-window (`src/web_handlers.cpp` + `meterReadings()`)
   - `spectrumWireQuantize` saturate (`src/spectrum_wire.h`)
3. Grep README for claim-unsafe language (grid-tie / dashboard / census /
   `aux_alert`); operator copy stays claim-safe.
4. Classify every finding into exactly one evidence tier.

---

## Evidence tiers

| Tier | Meaning | Action |
|---|---|---|
| Host-proven | Covered by native tests | Cite; do not claim ISR/OUTEN proof |
| Code-inspected | Firmware contract read on host; not native-tested | Do not treat as OUTEN/ISR evidence |
| Claim-safe docs | Operator copy matches code without bench proof | Docs PR only if drift |
| Bench-required | Needs disconnected scope / pin / timing evidence | `BENCH_CHECKS` campaign |
| Stay-off | Contested or architectural | Refuse software churn |

---

## Findings (by tier)

### Bench-required (operator risk)

| ID | Surface | Why still open |
|---|---|---|
| POL-2-5 | Polarity | Schemes 2 / 5 / 4-POD/APOD MASK+fault unscoped on inverted cell (`BENCH_CHECKS` §0a). Software clears POL before MASK; hardware proof missing. |
| BENCH-1 | Fault/OUTEN | Fault during authenticated clear — prove release-commit race on scope. |
| P0-3 | Fault/OUTEN | CMP→XBARA1→FAULT0 unconfirmed. Tm3/Tm4 software-only. |
| P0-2 | Superloop | 200 kHz ISR WCET is a budget; measure DWT ISR maxima (`vIsrCycles`) under the enabled-feature stress matrix, not only `missedIsrCycles`. |
| THERM-LIVE | Thermal | Fail-closed / keep-sample / mask-before-scan are coded; live harvest and release-wait need hardware (`§0c`). |
| MTP-CLR | MTP | `mtpAllowsPwmRelease` gates on `firstLoopDone`; prove 20 Hz timer not armed across release. |
| IMP-MQTT | Secrets | Identity gate is host-tested; confirm password does not follow a changed MQTT host on device. |
| PREFLIGHT | Boot | Preflight / complementary / provisioning rows in `BENCH_CHECKS` still unchecked. |

### Stay-off (refuse from this pass)

| ID | Why |
|---|---|
| USB-COMP | Bare `-DUSB_MTPDISK_SERIAL` stays; no `teensy41-pwm`. |
| DELAY-1 | Keep `delay(1)` (QNEthernet yield); do not use bare `yield()`. |
| APPLY-FAN | Do not strip capture / thermal / PLL from `applyPwmConfig`. |
| FLAGS-WRAP | Do not `lib_ignore` / `#if` call sites from `TEG_WITH_*=0` yet. |
| MARCH | Full 7.3 MiB EXTMEM March stays deferred (1 KiB prove already landed). |

### Host-proven contracts (still solid — not ship proof)

| ID | Contract |
|---|---|
| THERM-MATH | Derate math + `thermalAllowsPwmRelease` (host-tested). |
| RESTORE | `restoreSecrets` always restores write PIN; MQTT/Influx only on matching endpoint. |
| TEGS | `spectrumWireQuantize` saturates at 65535. |
| LITE-METER | `/api/status?lite=1` emits last-window `meterActive` without `analogRead`. |

### Code-inspected (not host-proven — not OUTEN proof)

| ID | Contract |
|---|---|
| REL-GEN | `releaseOutputInhibit` refuses OUTEN if `vFaultGeneration` moved; remask after connect. Code-inspected only; hardware proof is BENCH-1. |
| THERM-OW | OneWire skipped while OUTEN live. Code-inspected; live harvest/release-wait remains THERM-LIVE. |

### Claim-safe docs (no drift found this pass)

| ID | Note |
|---|---|
| MQTT-17 | 16 `MqttSensors[]` + fault; no `aux_alert`; energy `device_class` language without HA dashboard product. |
| README-UI | Stills under `docs/images/readme-ui-*.png`; fixture regenerator; captions say fixture / inhibited. |
| PLL-REF | README states bench reference lock, not grid-tie. |

---

## Bench-first queue (unchanged)

1. Fault pin assert during authenticated clear — outputs must stay off.
2. Enable thermal, confirm release waits for a probe, then generate.
3. Enable MTP, then clear a fault — 20 Hz timer must not stay armed.
4. Import a config with a different MQTT host — password must not follow.
5. §0a polarity schemes 2 / 5 / 4-POD/APOD on an inverted cell — pins go **LOW** on trip.

---

## What this pass did **not** do

- No firmware behavior change.
- No `plan/refactor-adversarial-fixes-7.md`.
- No GitHub issue dump as a software backlog.
- No ticking of `BENCH_CHECKS` from host results.
- No CMSIS FFT, global `-O3`, bare USB, or `yield()` swap proposals.

---

## PR policy after this note

| Class | Gate |
|---|---|
| Behavioral firmware from re-audit | NO-GO |
| Fresh host-testable regression | Conditional GO + native tests |
| Docs/comments claim drift | Feature-branch PR |
| Bench evidence | Update `BENCH_CHECKS` after real hardware work |
