Teensyduino 1.62 `MTP_Teensy.cpp` / `MTP_Storage.cpp` with the local
read-only and watchdog patches. `scripts/skip_core_mtp.py` compiles these
instead of `cores/teensy4` originals. See `lib/MTP_Teensy/PATCHES.md`.
